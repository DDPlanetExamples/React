import styled from "styled-components";

const Main = styled.div`
  user-select: none;
  padding: 0 15px;
  background: #fff;
  margin-bottom: 10px;

  &:last-child {
      margin-bottom: 0;
  }
`;

export default Main
